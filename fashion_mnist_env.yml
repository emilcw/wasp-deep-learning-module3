name: fashion_mnist_env
channels:
  - defaults
  - conda-forge
  - pytorch
dependencies:
  - python=3.11
  - numpy
  - matplotlib
  - scipy
  - pillow
  - tqdm
  - pip
  - pytorch
  - torchvision
  - pip:
      # Add any pip-only packages here if needed
      # e.g., somepackage